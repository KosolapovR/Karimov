<meta charset="UTF-8" />
<div><b>{{ message.user.name }}</b>, Вам пришло сообщение по товару
    {% if (message.product is null) %}
        {{ message.ad.name }}
    {% else %}
        {{ message.product.name }}
    {% endif %}
    {% if(message.author is defined) %}
    от {{ message.author }}
{% endif %}:
</div>
<p>
    {{ message.text }}
</p>
